# Godot 4 editor/import cache — regenerated automatically by the editor or by
# `godot --headless --import`, so it should not be committed. See the CI
# workflows under .github/workflows/, which regenerate it before exporting.
.godot/

# Per-resource import metadata, also regenerated by the editor/import step.
*.import
